name: Terraform LocalStack

# CI sequencing (avoids Kind+LocalStack Docker/CPU contention on small runners):
#   1) LocalStack alone + latency smoke
#   2) Configure backend (local | s3 | cloud) — s3 bootstraps on LocalStack
#   3) Terraform shared → network → backend  (no Kind)
#   4) Kind (+ metrics-server) + latency smoke again
#   5) Terraform eks + verify-apply
# Local `./scripts/lifecycle/up.sh` still starts Kind then LocalStack for one-command DX.
#
# Backend selection:
#   - workflow_dispatch: choose via "backend" input (default s3)
#   - push / pull_request: BACKEND=s3 (same default; override by editing env.BACKEND)

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment
        required: true
        type: choice
        options:
          - dev
          - staging
          - production
        default: staging
      action:
        description: Terraform action
        required: true
        type: choice
        options:
          - plan
          - apply
          - destroy
        default: plan
      backend:
        description: Backend type
        required: true
        type: choice
        options:
          - local
          - s3
          - cloud
        default: s3

concurrency:
  group: terraform-localstack-${{ github.event.inputs.environment || 'staging' }}-${{ github.event.inputs.backend || 's3' }}-${{ github.ref }}
  cancel-in-progress: false

env:
  TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN_app_terraform_io }}
  AWS_ACCESS_KEY_ID: test
  AWS_SECRET_ACCESS_KEY: test
  AWS_DEFAULT_REGION: ap-southeast-3
  AWS_EC2_METADATA_DISABLED: "true"
  TFC_ORG: ExperimentTerraform
  # workflow_dispatch uses the input; push/PR fall back to s3
  BACKEND: ${{ github.event.inputs.backend || 's3' }}
  # Lambda invoke smoke tests need the docker executor; Kind is deferred (below)
  # so both are not contending during shared/network/backend.
  LAMBDA_RUNTIME_EXECUTOR: docker

jobs:
  static-analysis:
    name: Static analysis (tflint + checkov)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.5.7"
          terraform_wrapper: false

      - name: Sync live stacks (local backend for lint)
        run: |
          chmod +x scripts/*/*.sh
          # Lint against generated local-backend roots; apply job configures the real backend.
          BACKEND=local ./scripts/lifecycle/sync-live.sh

      - name: ShellCheck
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq shellcheck
          shellcheck -S warning -x -s bash scripts/*/*.sh

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: v0.50.3

      - name: TFLint
        run: |
          tflint --init
          failed=0
          for dir in terraform/modules/*; do
            [ -d "$dir" ] || continue
            # Docs-only snippet directory (no real Terraform root module)
            [ "$(basename "$dir")" = "_provider" ] && continue
            echo "==> tflint $dir"
            if ! tflint --chdir="$dir" --format compact --config "$PWD/.tflint.hcl"; then
              failed=1
            fi
          done
          # Live stacks are generated; report but do not fail the job on unused shared vars noise
          for dir in terraform/live/staging/*; do
            [ -d "$dir" ] || continue
            echo "==> tflint $dir (informational)"
            tflint --chdir="$dir" --format compact --config "$PWD/.tflint.hcl" || true
          done
          exit "$failed"

      - name: Checkov
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: terraform/modules
          framework: terraform
          soft_fail: true
          skip_check: CKV_AWS_144,CKV_AWS_18,CKV_AWS_21,CKV2_AWS_6,CKV_AWS_145,CKV_AWS_19,CKV_AWS_26,CKV_AWS_27,CKV_AWS_7

  terraform:
    name: Terraform (${{ github.event.inputs.environment || 'staging' }} / ${{ github.event.inputs.backend || 's3' }})
    needs: static-analysis
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: "v1.29.14"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.5.7"
          terraform_wrapper: false

      - name: Resolve environment, action & backend
        id: meta
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "environment=${{ github.event.inputs.environment }}" >> "$GITHUB_OUTPUT"
            echo "action=${{ github.event.inputs.action }}" >> "$GITHUB_OUTPUT"
            echo "backend=${{ github.event.inputs.backend }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "environment=staging" >> "$GITHUB_OUTPUT"
            echo "action=plan" >> "$GITHUB_OUTPUT"
            echo "backend=${BACKEND}" >> "$GITHUB_OUTPUT"
          else
            echo "environment=staging" >> "$GITHUB_OUTPUT"
            echo "action=apply" >> "$GITHUB_OUTPUT"
            echo "backend=${BACKEND}" >> "$GITHUB_OUTPUT"
          fi

      - name: Persist BACKEND for later steps
        run: |
          echo "BACKEND=${{ steps.meta.outputs.backend }}" >> "$GITHUB_ENV"
          echo "Resolved BACKEND=${{ steps.meta.outputs.backend }} action=${{ steps.meta.outputs.action }} env=${{ steps.meta.outputs.environment }}"

      - name: Require TFC token when BACKEND=cloud
        if: steps.meta.outputs.backend == 'cloud'
        run: |
          if [ -z "${TF_TOKEN_app_terraform_io}" ]; then
            echo "::error::BACKEND=cloud requires repository secret TF_TOKEN_app_terraform_io."
            exit 1
          fi

      - name: Start LocalStack (no Kind yet)
        timeout-minutes: 5
        run: |
          chmod +x scripts/*/*.sh
          # Kind-independent stacks must not share the runner with a 3-node Kind cluster.
          SKIP_KIND=1 ./scripts/lifecycle/up.sh
          curl -sf http://localhost:4566/_localstack/health | tee /tmp/ls-health.json
          echo
          echo "LocalStack is healthy (Kind deferred until eks stage)."

      - name: Configure Terraform backend
        timeout-minutes: 5
        run: |
          chmod +x scripts/*/*.sh
          ENV="${{ steps.meta.outputs.environment }}"
          case "${{ steps.meta.outputs.backend }}" in
            s3)
              ./scripts/backend/use-s3-backend.sh "$ENV"
              ;;
            cloud)
              ./scripts/backend/use-tfc-backend.sh
              ;;
            local)
              ./scripts/backend/use-local-backend.sh
              ;;
            *)
              echo "::error::Unknown backend '${{ steps.meta.outputs.backend }}' (expected local|s3|cloud)"
              exit 1
              ;;
          esac

      - name: LocalStack latency smoke (pre-Kind)
        timeout-minutes: 2
        run: ./scripts/checks/check-localstack-latency.sh 5

      - name: Terraform ${{ steps.meta.outputs.action }} (shared,network,backend)
        if: steps.meta.outputs.action != 'destroy'
        timeout-minutes: 25
        run: |
          chmod +x scripts/*/*.sh
          ENV="${{ steps.meta.outputs.environment }}"
          ACTION="${{ steps.meta.outputs.action }}"
          # backend's terraform_remote_state needs shared+network state in the
          # configured backend. On plan (and fresh S3), apply upstream first;
          # env.sh also enforces this, but keep it explicit for CI clarity.
          ./scripts/lifecycle/env.sh "$ENV" apply shared,network
          ./scripts/lifecycle/env.sh "$ENV" "$ACTION" backend
          if [ "$ACTION" = "plan" ]; then
            ./scripts/lifecycle/env.sh "$ENV" plan shared,network
          fi

      - name: Start Kind (before eks only)
        timeout-minutes: 15
        run: |
          chmod +x scripts/*/*.sh
          ./scripts/kind/kind-up.sh

          # Kind create can briefly starve LocalStack. Wait/recover WITHOUT
          # docker network connect or compose restart (connect breaks host :4566
          # on Linux CI; restart wipes PERSISTENCE=0 state after early applies).
          # Kind→LocalStack uses host.docker.internal (see localstack-network-info.sh).
          if ! curl -sf --max-time 3 http://localhost:4566/_localstack/health >/dev/null; then
            echo "LocalStack unreachable after Kind create — docker compose up -d (no recreate)..."
            docker compose up -d --no-recreate
          fi
          for i in $(seq 1 60); do
            if curl -sf --max-time 3 http://localhost:4566/_localstack/health >/dev/null; then
              echo "LocalStack healthy (host :4566)."
              break
            fi
            if [[ "$i" -eq 60 ]]; then
              echo "::error::LocalStack did not recover after Kind start"
              docker compose ps || true
              docker compose logs --tail=80 || true
              exit 1
            fi
            sleep 2
          done

          kind get clusters
          kubectl --kubeconfig .kube/kind-config get nodes

      - name: LocalStack latency smoke (post-Kind)
        timeout-minutes: 2
        run: ./scripts/checks/check-localstack-latency.sh 5

      - name: Terraform ${{ steps.meta.outputs.action }} (eks)
        if: steps.meta.outputs.action != 'destroy'
        timeout-minutes: 25
        run: |
          chmod +x scripts/*/*.sh
          ./scripts/lifecycle/env.sh "${{ steps.meta.outputs.environment }}" "${{ steps.meta.outputs.action }}" eks

      - name: Verify apply
        if: steps.meta.outputs.action == 'apply'
        timeout-minutes: 15
        run: ./scripts/checks/verify-apply.sh "${{ steps.meta.outputs.environment }}"

      # Destroy needs Kind for eks first, then Kind-independent stacks (env.sh reverses order).
      - name: Terraform destroy (eks → backend → network → shared)
        if: steps.meta.outputs.action == 'destroy'
        timeout-minutes: 30
        run: |
          chmod +x scripts/*/*.sh
          ./scripts/lifecycle/env.sh "${{ steps.meta.outputs.environment }}" destroy

      - name: Diagnostics (on failure)
        if: failure()
        timeout-minutes: 5
        run: |
          echo "==> BACKEND=${BACKEND:-unset} LOCALSTACK_ENDPOINT=${LOCALSTACK_ENDPOINT:-unset}"
          echo "==> LocalStack health (localhost)"
          curl -sf --max-time 3 http://localhost:4566/_localstack/health || echo "(localhost failed)"
          echo
          LS_IP="$(docker inspect -f '{{range $n,$c := .NetworkSettings.Networks}}{{if ne $n "kind"}}{{println $c.IPAddress}}{{end}}{{end}}' testinfra-localstack 2>/dev/null | awk 'NF{print; exit}')"
          echo "==> LocalStack health (container IP ${LS_IP:-none})"
          if [ -n "$LS_IP" ]; then
            curl -sf --max-time 3 "http://${LS_IP}:4566/_localstack/health" || echo "(container IP failed)"
          fi
          echo
          echo "==> Docker stats (top)"
          docker stats --no-stream || true
          echo "==> LocalStack logs (tail)"
          docker compose logs --tail=120 || true
          echo "==> Kind nodes (if any)"
          kind get clusters || true
          kubectl --kubeconfig .kube/kind-config get nodes -o wide 2>/dev/null || true
          kubectl --kubeconfig .kube/kind-config get pods -A 2>/dev/null || true

      - name: Stop Kind + LocalStack
        if: always()
        timeout-minutes: 5
        run: |
          docker compose down -v || true
          # No-op safe if Kind never started (failed before eks stage).
          ./scripts/kind/kind-down.sh || true
